time series (mutli-variate vs univariate )
we are mainly interested in local parts of the time series using sliding windows.
interested into the similarity or dissimilarity, for that we compute distance (euclidian distance, area between two time series eg. )
motifs : recurrent pattern (every day) <>discord (un endroit qui a rien a voir, subsequent with the maximum distance regarding the other ones)
matrice profile (compare distance form every segment to every other one) distance matrix. easy way to find discord by geting the matrix and see the maximum. 